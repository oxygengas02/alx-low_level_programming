#include <stdio.h>

/**
 * main - print the phrase "programming is like building multilingual puzzle
 *
 * return : 0 if exited properly, non-zero otherwise
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
